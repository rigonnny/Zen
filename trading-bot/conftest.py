# This file is intentionally (almost) empty. Its presence tells pytest that
# THIS folder is the project root, which puts the `bot` package on Python's
# import path so tests can do `from bot.data import storage`.
